server {
    listen 80;
    server_name bazzar.yourdomain.com;

    # Redirect to HTTPS (uncomment when SSL is ready)
    # return 301 https://$server_name$request_uri;

    location /api/ {
        proxy_pass http://app:8080;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;
        client_max_body_size 50M;
    }

    location / {
        proxy_pass http://app:8080;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }
}

# HTTPS config (uncomment after certbot setup)
# server {
#     listen 443 ssl http2;
#     server_name bazzar.yourdomain.com;
#
#     ssl_certificate /etc/letsencrypt/live/bazzar.yourdomain.com/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/bazzar.yourdomain.com/privkey.pem;
#
#     location /api/ {
#         proxy_pass http://app:8080;
#         proxy_http_version 1.1;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         client_max_body_size 50M;
#     }
#
#     location / {
#         proxy_pass http://app:8080;
#         proxy_set_header Host $host;
#     }
# }
